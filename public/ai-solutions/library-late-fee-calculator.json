{"v":1,"m":"stealth/ox-alpha","t":"#3687 Library Late Fee Calculator — Tiered Penalties","o":"Pure simulation: for each book apply the tiered rule (1 day → 1; 2–5 days → 2·d; >5 days → 3·d) and sum. Constraints are tiny (≤100 books), so clarity beats cleverness.","k":["Three-way tier selection per element, then sum.","No accumulation pitfalls at these sizes (max total 100·300 = 30000)."],"a":[{"n":"Approach 1: Explicit tier branches","i":"Loop over daysLate, add the penalty for the matching tier.","s":["Initialize total = 0.","For each d: add 1 if d == 1, 2*d if 2 ≤ d ≤ 5, else 3*d.","Return total."],"c":{"python":"class Solution:\n    def lateFee(self, daysLate):\n        total = 0\n        for d in daysLate:\n            if d == 1: total += 1\n            elif d <= 5: total += 2 * d\n            else: total += 3 * d\n        return total","cpp":"class Solution {\npublic:\n    int lateFee(vector<int>& daysLate) {\n        int total = 0;\n        for (int d : daysLate) {\n            if (d == 1) total += 1;\n            else if (d <= 5) total += 2 * d;\n            else total += 3 * d;\n        }\n        return total;\n    }\n};","java":"class Solution {\n    public int lateFee(int[] daysLate) {\n        int total = 0;\n        for (int d : daysLate) {\n            if (d == 1) total += 1;\n            else if (d <= 5) total += 2 * d;\n            else total += 3 * d;\n        }\n        return total;\n    }\n}","javascript":"var lateFee = function(daysLate) {\n    let total = 0;\n    for (const d of daysLate) {\n        if (d === 1) total += 1;\n        else if (d <= 5) total += 2 * d;\n        else total += 3 * d;\n    }\n    return total;\n};"},"t":"O(n) — one pass","sp":"O(1)"},{"n":"Approach 2: Nested-ternary one-liner","i":"Same logic expressed as a sum of conditional expressions — compact and equally correct.","s":["Map each d to its penalty via a ternary chain.","Sum and return."],"c":{"python":"class Solution:\n    def lateFee(self, daysLate):\n        return sum(1 if d == 1 else 2 * d if d <= 5 else 3 * d for d in daysLate)","cpp":"class Solution {\npublic:\n    int lateFee(vector<int>& daysLate) {\n        int total = 0;\n        for (int d : daysLate) total += d == 1 ? 1 : (d <= 5 ? 2 * d : 3 * d);\n        return total;\n    }\n};","java":"class Solution {\n    public int lateFee(int[] daysLate) {\n        int total = 0;\n        for (int d : daysLate) total += d == 1 ? 1 : (d <= 5 ? 2 * d : 3 * d);\n        return total;\n    }\n}","javascript":"var lateFee = function(daysLate) {\n    return daysLate.reduce((t, d) => t + (d === 1 ? 1 : d <= 5 ? 2 * d : 3 * d), 0);\n};"},"t":"O(n)","sp":"O(1)"}],"w":"Trivial problem — the only risks are boundary slips (d == 1 vs d ≤ 5 ordering, strict > 5 for the top tier). Verify with the samples: [5,1,7] → 10+1+21 = 32. Follow-up: cap per-book penalties, or progressive daily rates."}